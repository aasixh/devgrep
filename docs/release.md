# Release Process

This document describes how devgrep releases are versioned, built, tagged, and published.

devgrep currently uses a manual release workflow to keep the process simple, transparent, and reproducible.

---

# Versioning

devgrep follows semantic versioning:

```text
vMAJOR.MINOR.PATCH
